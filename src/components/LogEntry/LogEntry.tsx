import React from 'react';
import './LogEntry.css';

const LogEntry: React.FC = () => (
  <div className="LogEntry" data-testid="LogEntry">
    LogEntry Component
  </div>
);

export default LogEntry;
